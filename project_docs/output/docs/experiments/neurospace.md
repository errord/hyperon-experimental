---
source_version: "0.2.10"
last_updated: "2026-03-23"
---

# Neurospace 实验

## 位置

`python/sandbox/neurospace/neurospace.py` 与 `python/sandbox/neurospace/test_nspace.metta`。

## 目标

探索神经符号结构（如图网络、嵌入空间）作为 **Atom Space** 后端，或与 **DAS** 类索引协同。

## 依赖

可能涉及可选 **protobuf**、外部服务；默认 **CI** 不一定执行。

## 注意

API 与命名空间 (`neurospace`) 均为实验性质；升级 **Hyperon** 时请预期破坏性变更。

## 参见

`others.md`、`../rust-engine/` 下与 **DAS** 相关文档（若已生成）
